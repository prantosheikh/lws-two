import { initailState } from "./initailState";
import {
	ADDED,
	ALLCOMPLETED,
	CLEARCOMPLETED,
	COLORSELECTED,
	DELETED,
	TOGGLED,
} from "./actionTypes";

const reducer = (state = initailState, action) => {
	switch (action.type) {
		case state:
			break;

		default:
			break;
	}
};
